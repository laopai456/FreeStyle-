<Window x:Class="FS服装搭配专家v1._0.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认"
        Height="140"
        Width="550"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    <Window.Resources>
        <SolidColorBrush x:Key="CardBackgroundColor" Color="#F5F5F5" />
        <SolidColorBrush x:Key="CardBorderColor" Color="#E0E0E0" />
        <SolidColorBrush x:Key="TextColor" Color="#7E57C2" />
        <SolidColorBrush x:Key="ButtonBackgroundColor" Color="#E0E0E0" />
        <SolidColorBrush x:Key="ButtonHoverColor" Color="#D0D0D0" />
        
        <Style x:Key="GlassButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBackgroundColor}" />
            <Setter Property="Foreground" Value="{StaticResource TextColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderColor}" />
            <Setter Property="Padding" Value="20,8" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="500" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontWeight="{TemplateBinding FontWeight}"
                                              TextElement.FontSize="{TemplateBinding FontSize}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource ButtonHoverColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F5F5F5" Offset="0" />
                <GradientStop Color="#E8E8E8" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>
        
        <Border Background="{StaticResource CardBackgroundColor}"
                CornerRadius="16"
                Margin="20"
                BorderThickness="2"
                BorderBrush="{StaticResource CardBorderColor}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10"
                                  ShadowDepth="3"
                                  Opacity="0.15"
                                  Direction="270"
                                  Color="#000000" />
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" 
                            VerticalAlignment="Center" 
                            HorizontalAlignment="Center"
                            Margin="20">
                    <TextBlock x:Name="MessageText"
                               Text="确认执行此操作吗？"
                               FontSize="14"
                               FontWeight="Normal"
                               Foreground="{StaticResource TextColor}"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               MaxWidth="450" />
                </StackPanel>
                
                <StackPanel Grid.Row="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            Margin="0,0,0,16">
                    <Button Content="取消" 
                            Style="{StaticResource GlassButton}"
                            Click="Cancel_Click" />
                    <Button Content="确定" 
                            Style="{StaticResource GlassButton}"
                            Click="Confirm_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>