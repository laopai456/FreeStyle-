<Window x:Class="FS服装搭配专家.UI.Windows.ItemSearchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="搜索道具" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#f8f8f8">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TxtSlotName" Text="搜索: 发型" FontWeight="Bold"
                   FontSize="14" Margin="0,0,0,8"/>

        <TextBox Grid.Row="1" x:Name="TxtSearch" FontSize="14" Padding="5"
                 TextChanged="TxtSearch_TextChanged"/>

        <ListBox Grid.Row="2" x:Name="LstResults" Margin="0,8" FontSize="13"
                 MouseDoubleClick="LstResults_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="{Binding Name}" Width="200" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Code}" Foreground="Gray" Width="80"/>
                        <TextBlock Text="{Binding Pak}" Foreground="LightGray" Width="50"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Padding="20,5" Margin="5,0" Click="BtnOk_Click"
                    Background="#4CAF50" Foreground="White" BorderThickness="0"/>
            <Button Content="取消" Padding="20,5" Margin="5,0" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
