# 下一步计划

## 当前状态

| 组件 | 状态 |
|------|------|
| 驱动文件 hwbp_driver.sys | ✅ 存在 (16KB) |
| VEH DLL veh_hook.dll | ✅ 存在 (104KB) |
| 注入工具 inject_dll.exe | ✅ 存在 |
| 驱动服务 HwBpDriver | ⚠️ 已注册但 STOPPED |
| 测试签名模式 | ❌ BCD存储损坏，bcdedit无法执行 |

## 卡点

BCD存储异常，无法通过命令行开启测试签名。

## 方案B：高级启动菜单禁用签名强制

1. 打开设置 → 恢复 → 高级启动 → 点击"立即重启"
2. 重启后进入蓝菜单 → 疑难解答 → 高级选项 → 启动设置 → 点击重启
3. 按 F7 选择"禁用驱动程序强制签名"
4. 进入系统后，管理员PowerShell执行：
   ```
   sc.exe start HwBpDriver
   ```
5. 测试驱动连接：
   ```
   python D:\py\反编译\FreeStyle\apollo_dump\hwbp_driver\hwbp_driver_client.py
   ```