# Progress 2026-06-01: §106 练习场路径追踪方案

> 目标：找出练习场阶段 AcquireSMD 的 ItemCode 来源

## 一、问题确认

从 `sprintf_replace_20260601_010024.txt` 日志确认：

| 阶段 | sprintf | AcquireSMD 路径 | ItemCode |
|------|---------|-----------------|----------|
| 房间 #8 | ✅ 触发，替换成功 | res768\i50125711_FN.smd | 50125711 (DST) ✅ |
| 过渡 #18,#28,#38 | ✅ 触发，替换成功 | res768\i50125711_FN.smd | 50125711 (DST) ✅ |
| **练习场 #58** | **❌ 零调用** | res767\i50125461_FN.smd | **50125461 (SRC)** ❌ |

**结论**：练习场有第二条加载路径，不经过 sprintf，直接用原始 ItemCode 调 AcquireSMD。

## 二、内存状态确认

从 `scan_diff_20260601_004126.txt`：
- 房间阶段：12 个地址含 SRC_IC
- 练习场阶段：13 个地址（多 1 个新地址）
- **SRC_IC 没有被清零**，12 个地址全部还在

**推论**：练习场不是从这些地址读取 ItemCode，而是有其他数据来源。

## 三、可能的数据来源

1. **服务器下发的装备数据** — 进房间时服务器发送，练习场直接用缓存
2. **预加载的资源缓存** — 房间阶段加载的模型被缓存，练习场用原始 ItemCode 引用
3. **练习场专用加载函数** — 不走 SetCharacterFeature/sprintf 路径

## 四、诊断脚本（已创建）

### 4.1 trace_practice_load.py
- **目标**：追踪练习场加载路径
- **方法**：监控 CreateFileA/W + ReadFile，记录文件访问序列
- **安全**：只 hook 系统 DLL
- **用法**：`python trace_practice_load.py`

### 4.2 trace_itemcode_reader.py
- **目标**：追踪谁在构造含原始 ItemCode 的路径
- **方法**：监控 CreateFileA/W，捕获含 50125461 的路径 + 20帧调用栈
- **安全**：只 hook 系统 DLL
- **用法**：`python trace_itemcode_reader.py`

### 4.3 trace_acquire_caller.py
- **目标**：追踪 AcquireSMD 完整调用链
- **方法**：hook AcquireSMD 调用者 (0x01EEBA30)，采样模式
- **风险**：游戏 .text hook，采样 50 次后自动停止
- **用法**：`python trace_acquire_caller.py`，观察后立即 quit

## 五、测试流程

### 步骤 1：安全诊断（推荐先做）
```
python trace_itemcode_reader.py
```
1. 进房间 → 观察文件路径和调用栈
2. 进练习场 → 关键：看有没有含 50125461 (SRC_IC) 的路径
3. 如果有 → 调用栈会显示谁在构造这个路径

### 步骤 2：加载路径追踪
```
python trace_practice_load.py
```
1. 进房间 → 观察 sprintf 和文件访问
2. 进练习场 → 观察静默期后的文件访问模式

### 步骤 3：高风险采样（谨慎）
```
python trace_acquire_caller.py
```
1. 进房间 → 观察 AcquireSMD 调用栈
2. 进练习场 → 关键：看 AcquireSMD #58 的调用栈差异
3. 采样满 50 次或观察到关键信息后立即 quit

## 六、预期结果

如果练习场有专用加载函数：
- trace_itemcode_reader 会捕获到含 SRC_IC 的文件路径
- 调用栈会显示与房间阶段不同的调用链
- 可以定位到练习场专用的数据入口函数

## 七、下一步方向

根据诊断结果：
- **如果找到专用函数**：分析是否可以安全 hook（冷函数？）
- **如果是服务器缓存**：考虑在网络层或缓存层替换
- **如果是资源预加载**：考虑在过渡阶段做二次替换

---

> 创建时间：2026-06-01
> 状态：诊断脚本就绪，等待测试