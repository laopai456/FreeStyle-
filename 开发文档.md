# FS服装搭配专家v1.0 开发文档

## 项目概述

FS服装搭配专家v1.0是一个用于街头篮球游戏服装搭配的工具，允许用户浏览、选择和搭配游戏中的服装物品。

**项目目录**：D:\py\反编译\FreeStyle

## 技术栈

- .NET 8.0 Windows Presentation Foundation (WPF)
- C#
- XAML
- 外部工具：UnFSPak (resources.exe) 用于提取游戏数据

## 编译配置

- 目标框架：.NET 8.0 Windows
- 输出目录：`bin\Debug\net8.0-windows`
- 注意：项目使用的是.NET 8.0，不是.NET 10，因此编译输出目录是`net8.0-windows`，不是`net10`

## 主要修改和修复

### 1. 路径加载和配置问题

**问题**：应用程序启动时没有询问加载路径，且无法正确加载游戏数据

**解决方案**：
- 增强了`InitializeConfig`方法，确保正确加载和保存配置文件
- 添加了路径验证逻辑，确保游戏安装目录存在且包含必要的文件
- 修改了路径构造方式，使用`AppDomain.CurrentDomain.BaseDirectory`代替`Environment.CurrentDirectory`，确保应用程序使用正确的目录路径

### 2. Pak文件提取问题

**问题**：`UnFSPak`工具(resources.exe)无法正确提取`item_text.pak`文件

**解决方案**：
- 确保`pack`目录包含`resources.exe`工具
- 修复了提取命令的路径构造，使用`Path.Combine`确保路径格式正确
- 添加了文件操作重试逻辑，处理文件锁定情况

### 3. 文件存在性检查问题

**问题**：`File.Exists`检查在某些情况下返回错误的结果，导致应用程序无法读取已存在的文件

**解决方案**：
- 移除了`File.Exists`检查，改为直接使用`using`语句打开文件
- 添加了异常处理，捕获文件不存在等错误情况
- 使用`using`语句确保资源正确释放，避免资源泄漏

### 4. 应用程序启动崩溃问题

**问题**：应用程序启动时崩溃，无法正常初始化

**解决方案**：
- 将数据加载从构造函数移到`Loaded`事件的后台线程中
- 添加了更详细的日志记录，便于调试和问题定位
- 优化了窗口初始化流程，确保UI元素正确加载

### 5. 图片加载问题

**问题**：点击加载图片按钮时出现错误，无法加载服装图片

**解决方案**：
- 修复了`LoadImages`方法中的路径构建问题
- 添加了自动加载服装数据的逻辑，如果列表为空
- 优化了图片加载流程，确保正确处理路径和文件存在性

### 6. 加载性能优化

**问题**：服装数据加载速度比原始源代码慢

**解决方案**：
- 移除了大量日志记录代码，减少I/O操作
- 优化了文件操作和重试逻辑，减少等待时间
- 实现了静默加载，移除了加载提示，提高了用户体验

### 7. 窗口大小设置问题

**问题**：尝试将窗口大小设置为1440x1080，但修改未生效

**解决方案**：
- 修改了`MainWindow.xaml`文件，将窗口大小从1400x720改为1440x1080
- 修改了`MainWindow.xaml.cs`文件，将构造函数中的窗口大小从1440x968改为1440x1080
- 重新编译了项目，但窗口大小修改仍然未生效

**根本原因**：`App.xaml.cs`中有硬编码的窗口大小设置，会覆盖`MainWindow`中的设置

**最终解决方案**（2026年3月3日）：
- 移除了`App.xaml.cs`中硬编码的窗口大小设置（原为800x600）
- 窗口大小现在统一在`MainWindow`中设置

**当前状态**：✅ 问题已解决

---

### 25. 视频背景支持（2026年3月10日）

**功能描述**：
- 支持主题使用视频作为背景
- 视频循环播放，支持静音设置
- 为后续自定义上传动态背景做准备

**实现方式**：
- `BackgroundStyle` 增加 `VideoPath` 和 `Volume` 属性
- `ThemeApplier` 新增 `IsVideoBackground()` 和 `GetVideoPath()` 方法
- `MainWindow.xaml` 添加 `MediaElement` 控件
- `MainWindow.xaml.cs` 添加视频播放控制和循环逻辑

**目录结构**：
```
项目目录/
├── videos/
│   └── dark-background.mp4    # 暗黑主题视频背景
└── ...
```

**主题配置示例**：
```json
{
  "styles": {
    "window": {
      "background": {
        "type": "video",
        "videoPath": "dark-background.mp4",
        "volume": 0
      }
    }
  }
}
```

**当前状态**：✅ 功能已实现

---

### 26. 主界面半透明调整（2026年3月10日）

**问题描述**：
- 暗黑主题的卡片背景不透明，完全遮挡视频背景
- 需要在透明度和可读性之间取得平衡

**解决方案**：
- 卡片背景调整为 40% 不透明度（`#40FFFFFF`）
- 内容面板背景调整为 20% 不透明度（`#20FFFFFF`）
- 边框调整为 50% 不透明度（`#50FFFFFF`）
- 阴影改为霓虹发光效果（颜色 `#6DD5FA`）

**当前状态**：✅ 问题已解决

---

### 27. 实时状态显示（2026年3月10日）

**功能描述**：
- 在服装数据加载过程中显示实时状态
- 方便用户理解当前操作进度，区分正常加载和程序卡死

**状态更新点**：
| 阶段 | 显示文本 |
|------|----------|
| 初始化 | "正在初始化..." |
| 检查文件 | "正在检查游戏目录文件..." |
| 复制 pak | "正在复制 item_text.pak..." |
| 解包 | "正在解包 item_text.pak..." |
| 解析数据 | "正在解析服装数据..." |
| 加载图片 | "正在加载图片..." |
| 解包图片 | "正在解包图片... {当前}/{总数}" |
| 完成 | 显示2秒后隐藏 |

**实现方式**：
- `MainWindow.xaml` 添加 `labLoadingStatus` 文本块
- `MainWindow.xaml.cs` 添加 `UpdateLoadingStatus()` 方法
- 加载流程各关键步骤调用状态更新

**当前状态**：✅ 功能已实现

---

### 28. SkinWindow 实心背景和主题适配（2026年3月10日）

**问题描述**：
- SkinWindow 使用透明背景导致文字看不清
- 暗黑主题下 SkinWindow 背景和文字都是白色

**解决方案**：
- SkinWindow 使用实心背景（不透明）
- 根据主题 ID 设置不同的背景和文字颜色

**各主题 SkinWindow 样式**：
| 主题 | 背景色 | 文字颜色 |
|------|--------|----------|
| 暗黑 | `#1A1A2E`（深蓝黑色） | 白色，标题霓虹青色 |
| 银河 | 白色 | 深灰色 |
| 默认 | `#F5F5F8`（浅灰白色） | 深灰色 |

**皮肤预览**：
- 视频背景主题显示深色渐变预览
- 其他主题正常显示主题背景

**当前状态**：✅ 问题已解决

---

## 窗口大小设置指南

如需修改窗口默认大小，需要同时修改以下位置：

### 1. MainWindow.xaml（第6-7行）
```xml
Height="768"
Width="1440"
```

### 2. MainWindow.xaml.cs（第70-71行）
```csharp
this.Width = 1440;
this.Height = 768;
```

### 3. App.xaml.cs（已移除硬编码）
之前在`OnStartup`方法中有硬编码设置：
```csharp
// 已移除，不再需要修改此处
mainWindow.Width = 800;
mainWindow.Height = 600;
```

**注意事项**：
- `MainWindow.xaml`和`MainWindow.xaml.cs`两处设置需保持一致
- `WindowState`属性控制窗口状态：`Normal`（正常）或`Maximized`（最大化）
- `WindowStartupLocation`属性控制窗口初始位置：`CenterScreen`（居中）

### 8. 服装列表显示乱码问题

**问题**：服装列表中的中文显示为乱码

**原因分析**：编码处理问题，itemshop.txt文件读取时的编码设置不正确

**根本原因**：
- itemshop.txt文件是GBK/GB2312编码，不是Big5编码
- v1.0错误地使用`Encoding.GetEncoding(950)`（Big5）读取文件
- v5.3.6源代码使用`Encoding.Default`读取文件，正常显示中文

**解决方案**：
- 注册编码提供程序：`Encoding.RegisterProvider(CodePagesEncodingProvider.Instance)`
- 使用GB2312编码读取文件：`Encoding.GetEncoding("GB2312")`
- 这是因为.NET 8.0中`Encoding.Default`是UTF-8，需要显式指定GB2312编码

**当前状态**：✅ 问题已解决

### 9. 实时搜索功能

**功能描述**：在搜索框中输入关键字，服装列表实时显示匹配的结果

**实现方式**：
- 使用`txtSearch_TextChanged`事件触发实时搜索
- 搜索范围包括：ItemCode、PakNum、EffectCode、ItemName、Comment、ItemType
- 使用`StringComparison.OrdinalIgnoreCase`进行不区分大小写的搜索
- 清空搜索框时恢复显示全部服装

**当前状态**：✅ 功能已实现

### 10. 服装选择功能

**功能描述**：
- 左键点击服装：添加到"变更前"列表
- Ctrl+左键点击服装：添加到"变更后"列表
- 点击已添加的服装：从列表中移除
- 确认变更：将配对的服装变更记录到dopaklog.ini
- 清空所有：清空变更前和变更后的服装列表
- 显示服装图标：变更前后区域显示服装图标而非文本
- 鼠标悬停提示：显示服装名称

**实现方式**：
- 使用`lstClothing_SelectionChanged`事件处理服装选择
- 通过`Keyboard.Modifiers`检测Ctrl键状态
- 使用`beforeItems`和`afterItems`列表存储选中的服装
- 动态更新UI面板显示选中的服装图标
- 使用`BitmapImage`加载图标，设置`CacheOption.OnLoad`避免文件锁定
- 配对状态显示已配对的服装数量

**当前状态**：✅ 功能已实现

### 11. 加载图片按钮

**功能描述**：
- 解包icon pak文件，加载服装图标
- 位置：搜索框右侧
- 支持解包cookies目录下已有的pak文件
- 支持从游戏目录复制缺失的pak文件并解包

**实现方式**：
- 在XAML中添加按钮，使用`WindowButton`样式
- 点击触发`btnLoadImg_Click`事件
- 先解包cookies目录下所有已有的icon*.pak文件
- 再检查服装列表中缺失的pak文件并从游戏目录复制解包
- 使用`conmon.RunCmd`执行解包命令

**关键代码**：
- `RunCmd`方法使用`StandardInput.WriteLine`执行命令（与v5.3.6一致）
- 解包命令：`pack\resources "pak文件路径" -byname .+\.png`

**当前状态**：✅ 功能已实现

### 12. 窗口最大化功能

**功能描述**：程序启动时自动最大化窗口

**实现方式**：
- 在XAML中设置`WindowState="Maximized"`
- 在代码中设置`this.WindowState = WindowState.Maximized`

**当前状态**：✅ 功能已实现

### 13. 服装变更功能

**功能描述**：
- 确认变更：执行实际的服装替换操作
- 批量变更：支持同时变更多件服装
- 变更记录：记录变更历史到dopaklog.ini
- 自动解包：选择服装时自动解包对应的item pak文件

**实现方式**：
- 选择服装时自动解包对应的item pak文件（UnpackItemPak方法）
- 复制服装模型文件（.bml）到目标item pak目录
- 使用resources.exe -file2pak打包回游戏目录
- 记录变更日志到dopaklog.ini

**核心流程**：
1. 选择服装时检查并解包对应的item pak文件
2. 确认变更时复制源服装模型文件到目标位置
3. 使用resources.exe -file2pak将目录打包回游戏pak文件
4. 记录变更日志

**关键问题：PakNum字段处理**：

`PakNum`字段格式为`*747.pak`（包含`*`前缀和`.pak`后缀），需要正确转换：

| 用途 | 转换方式 | 示例 |
|------|----------|------|
| pak文件名 | `item` + PakNum.Replace("*", "") | item747.pak |
| 解包目录名 | pak文件名.Replace(".", "_") | item747_pak |
| 游戏目录pak路径 | 游戏目录 + `\` + pak文件名 | C:\...\item747.pak |

**路径引号问题**：

游戏安装路径包含空格（如`C:\Program Files (x86)\...`），必须给路径加引号：

```csharp
// 正确：使用引号包裹路径
string cmd = "pack\\resources -file2pak \"" + beforePakDir + "\" \"" + pakPath + "\"";

// 错误：路径中的空格会导致参数解析错误
string cmd = "pack\\resources -file2pak " + beforePakDir + " " + pakPath;
```

**当前状态**：✅ 功能已实现

## 目录结构

```
D:\py\反编译\FreeStyle\
├── bin\Debug\net8.0-windows\  # 编译输出目录
│   ├── cookies\              # 游戏数据目录
│   │   ├── item_text.pak      # 游戏物品数据文件
│   │   └── item_text_pak\     # 提取后的数据文件
│   ├── pack\                 # 工具目录
│   │   └── resources.exe      # UnFSPak工具
│   ├── skins\                # 主题目录
│   │   ├── dark\             # 暗黑主题
│   │   ├── galaxy\           # 银河主题
│   │   └── ...
│   ├── app.config.json       # 应用配置文件
│   └── FS服装搭配专家v1.0.exe  # 可执行文件
├── UI\                        # UI相关代码
│   └── Windows\               # 窗口代码
│       └── MainWindow.xaml.cs  # 主窗口代码
├── Core\                      # 核心代码
│   ├── Config\               # 配置相关
│   │   ├── AppConfiguration.cs  # 配置数据模型
│   │   ├── ConfigService.cs     # 配置服务
│   │   └── AppConfig.cs         # 常量类
│   ├── Models\               # 数据模型
│   ├── Services\             # 服务类
│   └── Utilities\            # 工具类
├── FS服装搭配专家v1.0.csproj   # 项目配置文件
└── 开发文档.md                  # 本开发文档
```

## 配置文件

配置文件 `app.config.json` 用于存储应用程序的所有配置（2026年3月10日优化后）：

```json
{
  "game": {
    "installDirectory": "C:\\Program Files (x86)\\T2CN\\街头篮球",
    "mapLibraryPath": ""
  },
  "ui": {
    "currentTheme": "default",
    "consoleVisible": false,
    "windowWidth": 1440,
    "windowHeight": 768
  },
  "data": {
    "pakSizes": {
      "item747.pak": 12345678
    },
    "referenceBytes": {
      "default": "12345678"
    }
  }
}
```

**配置项说明**：

| 路径 | 说明 |
|------|------|
| game.installDirectory | 游戏安装目录 |
| game.mapLibraryPath | 地图库路径 |
| ui.currentTheme | 当前主题ID |
| ui.consoleVisible | 控制台是否显示 |
| ui.windowWidth | 窗口宽度 |
| ui.windowHeight | 窗口高度 |
| data.pakSizes | pak文件原始大小记录 |
| data.referenceBytes | 参考字节数据 |

**旧配置文件迁移**：

首次运行时，ConfigService 会自动从以下旧文件迁移数据：
- `config.ini` → `game.installDirectory`
- `console.ini` → `ui.consoleVisible`
- `current_skin.ini` → `ui.currentTheme`
- `pak_sizes.txt` → `data.pakSizes`

## 编译和运行

### 编译步骤

1. 打开命令行工具，进入项目目录：`cd D:\py\反编译\FreeStyle`
2. 执行编译命令：`dotnet build`
3. 编译成功后，可执行文件将生成在`bin\Debug\net8.0-windows`目录

### 运行步骤

1. 确保`config.ini`文件中设置了正确的游戏安装目录
2. 确保`pack`目录包含`resources.exe`工具
3. 运行`FS服装搭配专家v1.0.exe`可执行文件

## 常见问题和解决方案

### 1. 无法找到游戏安装目录

**症状**：应用程序启动时提示无法找到游戏安装目录

**解决方案**：
- 确保`config.ini`文件存在且包含正确的游戏安装目录路径
- 手动选择游戏安装目录，应用程序会自动保存到`config.ini`

### 2. 无法提取Pak文件

**症状**：应用程序提示无法提取`item_text.pak`文件

**解决方案**：
- 确保`pack`目录包含`resources.exe`工具
- 确保游戏安装目录包含`item_text.pak`文件
- 检查文件权限，确保应用程序有读取和写入权限

### 3. 无法加载图片

**症状**：点击加载图片按钮时出现错误

**解决方案**：
- 确保游戏安装目录包含必要的图片文件
- 确保应用程序有读取图片文件的权限
- 检查网络连接，某些图片可能需要从网络加载

## 日志文件

应用程序生成以下日志文件，用于调试和问题定位：

- `startup.log`：应用程序启动日志
- `init.log`：初始化日志
- `getnewitem.log`：服装数据加载日志
- `mainwindow.log`：主窗口日志

## 注意事项

1. 应用程序需要.NET 8.0运行时环境
2. 确保游戏安装目录包含必要的游戏数据文件
3. 定期备份`config.ini`文件，避免配置丢失
4. 如果遇到问题，查看日志文件获取详细信息

## 项目状态

✅ **已完成**：应用程序可以成功启动，加载游戏数据
✅ **已修复**：图片加载功能正常工作
✅ **已优化**：应用程序启动速度和稳定性得到提升
✅ **已修复**：服装列表显示乱码问题（2026年2月26日）
✅ **已实现**：实时搜索功能（2026年2月26日）
✅ **已实现**：服装选择功能，显示服装图标（2026年2月26日）
✅ **已实现**：加载图片按钮（2026年2月26日）
✅ **已实现**：窗口最大化功能（2026年2月26日）
✅ **已实现**：服装变更和批量变更功能（2026年2月26日）
✅ **已修复**：图片解包功能，使用与v5.3.6一致的RunCmd方法（2026年2月27日）
✅ **已实现**：item pak自动解包功能，选择服装时自动解包（2026年2月27日）
✅ **已实现**：变更功能完整实现，使用-file2pak打包回游戏目录（2026年2月27日）
✅ **已修复**：PakNum字段处理问题，正确移除`*`和`.pak`后缀（2026年2月27日）
✅ **已修复**：目录名格式统一为`itemXXX_pak`（2026年2月27日）
✅ **已更新**：确认对话框样式，改为横向显示，减小字体大小，调整为灰白底色和紫色文字（2026年2月28日）
✅ **已实现**：地图切换功能，支持地图库管理、预览、搜索和应用地图（2026年2月28日）
✅ **已修复**：确认对话框Topmost属性，解决主窗口遮挡问题（2026年3月1日）
✅ **已优化**：搜索框样式统一，添加圆角和玻璃拟态效果（2026年3月1日）
✅ **已修复**：config.ini配置格式统一为键值对格式（2026年3月1日）
✅ **已实现**：常用工具功能 - 背景修改和补充字节（2026年3月1日）
✅ **已实现**：控制台开关功能 - 滑动开关控制控制台显示（2026年3月8日）
✅ **已实现**：特效添加功能 - Alt+左键为服装添加特效（2026年3月8日）
✅ **已实现**：性能监控代码 - 诊断顿卡问题（2026年3月8日）
✅ **已修复**：取消窗口置顶设置（2026年3月10日）
✅ **已优化**：主题透明度平衡调整（2026年3月10日）
✅ **已修复**：隐藏 UnFSPak DOS 窗口（2026年3月10日）
✅ **已实现**：配置文件合并优化（2026年3月10日）
✅ **已实现**：常量类创建（2026年3月10日）
✅ **已实现**：子窗口主题接入（2026年3月10日）
✅ **已修复**：配置迁移功能（2026年3月11日）
✅ **已优化**：ConfirmDialog 样式（2026年3月15日）
✅ **已优化**：游戏目录设置引导（2026年3月11日）
✅ **已实现**：单实例检测（2026年3月11日）
✅ **已优化**：搜索性能优化（2026年3月11日）
✅ **已实现**：窗口尺寸配置化（2026年3月11日）
✅ **已实现**：UI元素尺寸统一样式（2026年3月11日）
✅ **已实现**：提示文本资源化（2026年3月11日）
✅ **已实现**：日志文件整合（2026年3月11日）
✅ **已实现**：Win32常量定义（2026年3月11日）
✅ **已实现**：尺寸常量定义与MainWindow字体优化（2026年3月11日）

### 49. 尺寸常量定义与MainWindow字体优化（2026年3月11日）

**功能描述**：
- 在 Styles.xaml 中定义统一的尺寸常量
- 将 MainWindow.xaml 中的硬编码字体大小替换为资源引用

**新增尺寸常量**：

| 类别 | 常量名 | 值 |
|------|--------|-----|
| 字体大小 | FontSize_Tiny | 11 |
| 字体大小 | FontSize_Small | 12 |
| 字体大小 | FontSize_Normal | 14 |
| 字体大小 | FontSize_Medium | 15 |
| 字体大小 | FontSize_Large | 16 |
| 字体大小 | FontSize_Title | 18 |
| 字体大小 | FontSize_Header | 24 |
| 字体大小 | FontSize_Display | 48 |
| 图标尺寸 | IconSize_Tiny | 14 |
| 图标尺寸 | IconSize_Small | 16 |
| 图标尺寸 | IconSize_Medium | 24 |
| 图标尺寸 | IconSize_Large | 32 |
| 图标尺寸 | IconSize_XLarge | 48 |
| 列表项 | ListItemHeight | 36 |
| 列表项 | ListItemIconSize | 80 |
| 标题栏 | TitleBarHeight | 50 |
| 标题栏 | TitleBarIconSize | 18 |
| 开关控件 | ToggleSwitchTrackWidth | 32 |
| 开关控件 | ToggleSwitchTrackHeight | 16 |
| 开关控件 | ToggleSwitchThumbSize | 14 |

**修改文件**：
- `UI/Resources/Styles.xaml` - 新增尺寸常量定义
- `UI/Windows/MainWindow.xaml` - 替换所有硬编码字体大小为资源引用

**当前状态**：✅ 功能已实现

---

### 44. 窗口尺寸配置化（2026年3月11日）

**功能描述**：
- 将所有窗口的默认尺寸存储到 `app.config.json`
- 支持用户自定义窗口尺寸并保存
- 实现窗口尺寸的记忆功能

**实现方式**：
- `AppConfiguration.cs` 添加窗口尺寸属性
- `ConfigService.cs` 添加窗口尺寸访问器
- 各窗口添加 `LoadWindowSize()` 方法和 `SizeChanged` 事件

**修改文件**：
- `Core/Config/AppConfiguration.cs` - 添加窗口尺寸属性
- `Core/Config/ConfigService.cs` - 添加窗口尺寸访问器
- `UI/Windows/MainWindow.xaml.cs` - 窗口尺寸加载和保存
- `UI/Windows/BgCropWindow.xaml.cs` - 窗口尺寸加载和保存
- `UI/Windows/FillBytesWindow.xaml.cs` - 窗口尺寸加载和保存
- `UI/Windows/SkinWindow.xaml.cs` - 窗口尺寸加载和保存
- `UI/Windows/TeamSelectionWindow.xaml.cs` - 窗口尺寸加载和保存

**当前状态**：✅ 功能已实现

---

### 45. UI元素尺寸统一样式（2026年3月11日）

**功能描述**：
- 创建统一的按钮、输入框、标签页样式
- 确保所有UI元素尺寸一致
- 减少硬编码的尺寸值

**实现方式**：
- 创建 `UI/Resources/Styles.xaml` 资源字典
- 定义统一的圆角、尺寸、间距常量
- 定义 GlassButton、GlassTextBox、GlassComboBox、GlassTabItem 等样式

**修改文件**：
- `UI/Resources/Styles.xaml` - 新建统一样式资源
- `UI/App.xaml` - 引用样式资源
- `FS服装搭配专家v1.0.csproj` - 包含样式文件

**当前状态**：✅ 功能已实现

---

### 46. 提示文本资源化（2026年3月11日）

**功能描述**：
- 将所有提示文本、错误消息等整合到资源文件
- 支持多语言（可选）
- 便于统一管理和修改

**实现方式**：
- 创建 `UI/Resources/Strings.xaml` 资源字典
- 定义通用提示文本、窗口标题、错误消息等

**修改文件**：
- `UI/Resources/Strings.xaml` - 新建文本资源
- `UI/App.xaml` - 引用文本资源
- `FS服装搭配专家v1.0.csproj` - 包含文本资源文件

**当前状态**：✅ 功能已实现

---

### 47. 日志文件整合（2026年3月11日）

**功能描述**：
- 将多个日志文件合并为一个结构化日志
- 添加日志级别控制
- 优化日志存储和清理机制

**实现方式**：
- `OperationDebugger.cs` 添加 `LogLevel` 枚举
- 添加 `CurrentLogLevel` 属性控制日志级别
- 添加 `LogRetentionDays` 属性控制日志保留天数
- 添加 `CleanupOldLogs()` 方法自动清理旧日志

**修改文件**：
- `Core/Utilities/OperationDebugger.cs` - 日志级别和清理功能

**当前状态**：✅ 功能已实现

---

### 48. Win32常量定义（2026年3月11日）

**功能描述**：
- 将 Win32 API 相关常量集中定义
- 提高代码可读性
- 便于维护和扩展

**实现方式**：
- 创建 `Core/Utilities/Win32Constants.cs` 文件
- 定义区域设置、窗口消息、文件操作、错误代码等常量
- 更新 `conmon.cs` 使用新常量

**修改文件**：
- `Core/Utilities/Win32Constants.cs` - 新建Win32常量定义
- `Core/Utilities/conmon.cs` - 使用新常量

**当前状态**：✅ 功能已实现

---

### 43. 搜索性能优化（2026年3月11日）

**问题描述**：
- 实时搜索时输入文字会顿卡
- 删除文字时顿卡更严重（结果列表从少变多，渲染更慢）
- 每次按键都触发搜索，导致频繁刷新

**解决方案**：

1. **防抖机制（Debounce）**：
   - 使用 `DispatcherTimer` 延迟搜索
   - 用户停止输入 200ms 后再执行搜索
   - 减少约 80% 的搜索次数

2. **异步搜索**：
   - 使用 `Task.Run` 在后台线程执行过滤
   - 使用 `CancellationTokenSource` 取消旧搜索
   - UI 线程不再阻塞

3. **虚拟化优化**：
   - 启用 `VirtualizingPanel.IsVirtualizing="True"`
   - 启用 `VirtualizingPanel.VirtualizationMode="Recycling"`
   - 设置 `ScrollViewer.CanContentScroll="True"`
   - 只渲染可见项，大幅提升渲染性能

**修改文件**：
- `MainWindow.xaml.cs` - 添加防抖和异步搜索逻辑
- `MainWindow.xaml` - GlassListBox 样式启用虚拟化

**关键代码**：
```csharp
// 防抖
private void txtSearch_TextChanged(object sender, TextChangedEventArgs e)
{
    if (_searchDebounceTimer == null)
    {
        _searchDebounceTimer = new DispatcherTimer();
        _searchDebounceTimer.Interval = TimeSpan.FromMilliseconds(200);
        _searchDebounceTimer.Tick += async (s, args) =>
        {
            _searchDebounceTimer.Stop();
            await SearchAsync();
        };
    }
    _searchDebounceTimer.Stop();
    _searchDebounceTimer.Start();
}

// 异步搜索
private async Task SearchAsync()
{
    _searchCts?.Cancel();
    _searchCts = new CancellationTokenSource();
    var token = _searchCts.Token;
    
    var filteredList = await Task.Run(() => 
        list.Where(item => MatchItem(item, searchText)).ToList(), token);
    
    if (!token.IsCancellationRequested)
    {
        lstClothing.ItemsSource = filteredList;
    }
}
```

**当前状态**：✅ 功能已实现

---

### 42. 单实例检测（2026年3月11日）

**问题描述**：
- 程序可以被多次启动，导致文件操作冲突
- 多个实例同时操作同一文件可能产生"副本"文件

**解决方案**：
- 使用 `Mutex` 实现单实例检测
- 程序启动时检测是否已有实例运行
- 如果已运行，弹窗提示并退出

**实现代码**：
```csharp
private static Mutex? _mutex;

[STAThread]
public static void Main(string[] args)
{
    bool createdNew;
    _mutex = new Mutex(true, "FS服装搭配专家v1.0_SingleInstance", out createdNew);
    
    if (!createdNew)
    {
        MessageBox.Show("程序已经在运行中！\n\n请检查任务栏或系统托盘。", "程序已运行", ...);
        return;
    }
    // ...
}
```

**修改文件**：
- `Program.cs` - 添加 Mutex 单实例检测

**当前状态**：✅ 功能已实现

---

### 41. 游戏目录设置引导优化（2026年3月11日）

**问题描述**：
- 新用户首次运行时，游戏目录为空，显示错误提示要求编辑 JSON 文件
- 对普通用户极不友好，用户不知道 app.config.json 在哪里
- 无图形界面设置游戏路径

**解决方案**：

1. **自动检测游戏路径**：
   - 检测常见安装路径（C/D/E/F 盘的 T2CN\街头篮球）
   - 从 Windows 注册表检测游戏安装路径
   - 检测到后弹窗确认

2. **文件夹选择对话框**：
   - 未检测到路径或用户拒绝时，弹出文件夹选择对话框
   - 验证所选目录是否包含 item_text.pak 文件
   - 选择无效目录时提示重新选择

3. **设置按钮**：
   - 标题栏添加设置按钮（⚙ 图标）
   - 用户可随时更改游戏目录

**新增方法**：

| 方法 | 说明 |
|------|------|
| `ConfigService.IsValidGamePath()` | 验证游戏路径是否有效 |
| `ConfigService.AutoDetectGamePath()` | 自动检测游戏安装路径 |
| `ConfigService.DetectFromRegistry()` | 从注册表检测游戏路径 |
| `ConfigService.SetGameInstallDirectory()` | 设置并保存游戏目录 |
| `ConfigService.NeedsGamePathSetup()` | 判断是否需要设置游戏目录 |
| `MainWindow.ShowFolderDialog()` | 显示文件夹选择对话框 |
| `MainWindow.btnSettings_Click()` | 设置按钮点击事件 |

**首次运行流程**：
```
启动程序
    ↓
读取 app.config.json
    ↓
游戏目录为空或无效？
    ├── 是 → 自动检测游戏路径
    │        ├── 找到 → 弹窗确认 → 保存并继续
    │        └── 未找到 → 弹出文件夹选择对话框
    │                    ├── 选择有效目录 → 保存并继续
    │                    └── 取消 → 显示错误提示
    │
    └── 否 → 继续加载
```

**修改文件**：
- `Core/Config/ConfigService.cs` - 添加路径检测和验证方法
- `UI/Windows/MainWindow.xaml.cs` - 添加首次运行引导逻辑
- `UI/Windows/MainWindow.xaml` - 添加设置按钮

**当前状态**：✅ 功能已实现

---

### 14. 地图切换功能

**功能描述**：
- 地图库管理：设置和保存地图库路径
- 地图列表：显示所有可用地图
- 地图预览：选中地图后显示预览图
- 地图搜索：按名称搜索过滤地图
- 应用地图：将选中的地图应用到游戏目录

**实现方式**：
- 创建 `MapM` 数据模型存储地图信息
- 创建 `MapControl` 用户控件实现地图切换界面
- 地图库路径保存在 `config.ini` 文件中
- 应用地图时直接复制 `stage02.pak` 文件到游戏目录

**地图文件结构**：
```
地图库目录/
├── 地图A/
│   ├── stage02.pak    # 必须存在
│   └── preview.png    # 预览图（png或jpg）
├── 地图B/
│   ├── stage02.pak
│   └── preview.jpg
└── ...
```

**当前状态**：✅ 功能已实现

### 15. 常用工具功能

**功能描述**：
- **背景修改**：选择图片，自动切割并替换游戏背景
  - 支持7种背景类型：登录界面、角色选择、大厅背景、等待房间、读取加载1/2/3
  - 自动将图片缩放到1366x768
  - 自动切割为512x512的块
  - 解包、替换、打包一体化

- **补充字节**：补充pak文件字节数到指定大小
  - 自动扫描cookies目录下的pak文件
  - 支持搜索过滤文件
  - 自动记录原始pak文件大小到pak_sizes.txt
  - 自动填充到原始文件大小
  - 支持手动设置目标字节数
  - 提供重新扫描功能更新原始大小记录

**实现方式**：
- 创建 `BgCropWindow` 窗口实现背景修改功能
- 创建 `FillBytesWindow` 窗口实现补充字节功能
- 使用 `conmon.RunCmd` 调用resources.exe进行解包/打包

**当前状态**：✅ 功能已实现

### 16. 换肤功能

**功能描述**：
- 主题切换：支持默认玻璃拟态、暗黑霓虹、银河星空三种主题
- 主题保存：选择的主题会被持久化保存
- 实时预览：主题预览窗口显示效果
- 独立图标：右上角独立的换肤图标
- 动态颜色：所有UI元素颜色通过 DynamicResource 绑定，支持实时切换

**实现方式**：
- 创建 `SkinTheme` 数据模型定义主题结构
- 创建 `ThemeLoader` 服务加载主题配置
- 创建 `ThemeApplier` 服务应用主题到UI
- 主题配置存储在项目目录的 `skins` 目录
- 选中的主题ID保存到 `current_skin.ini` 文件

**主题存储结构**：
```
项目目录/skins/
├── dark/                 # 暗黑主题
│   └── theme.json
├── galaxy/               # 银河星空主题
│   └── theme.json
└── current_skin.ini      # 当前主题ID
```

**可用主题**：

| 主题ID | 名称 | 特点 |
|--------|------|------|
| default | 默认玻璃拟态 | 渐变背景，半透明玻璃效果，白色文字 |
| dark | 暗黑霓虹 | 深色渐变背景，霓虹色调，白色文字 |
| galaxy | 银河星空 | 白色背景，紫色按钮，黑色文字 |

**银河星空主题特点**（2026年3月3日新增）：
- 白色纯色背景
- 灰色内容区域（#F5F5F5）
- 紫色按钮（#7E57C2）
- 按钮字体白色，其他文字黑色
- 选中标签页紫色背景配白色文字
- 未选中标签页透明背景配黑色文字

**主题配置格式**：
```json
{
  "id": "galaxy",
  "name": "银河星空",
  "author": "Developer",
  "version": "1.0",
  "description": "白灰底色配紫色按钮的冲突设计风格",
  "styles": {
    "window": {
      "background": {
        "type": "solid",
        "colors": ["#FFFFFF"],
        "angle": 0
      }
    },
    "card": {
      "background": "#F5F5F5",
      "borderColor": "#E0E0E0",
      "contentPanelBackground": "#F5F5F5",
      "cornerRadius": 16
    },
    "button": {
      "background": "#7E57C2",
      "hoverBackground": "#9575CD",
      "pressedBackground": "#5E35B1",
      "borderColor": "#7E57C2",
      "cornerRadius": 8,
      "foreground": "#FFFFFFFF"
    },
    "text": {
      "primary": "#424242",
      "secondary": "#757575",
      "title": "#424242",
      "body": "#424242"
    },
    "tab": {
      "background": "transparent",
      "selectedBackground": "#7E57C2",
      "hoverBackground": "#E8E0F0",
      "selectedForeground": "#FFFFFFFF",
      "cornerRadius": 8
    }
  }
}
```

**当前状态**：✅ 功能已实现

---

## UI设计规范

### 按钮规格

项目中的按钮分为三类，统一使用 `GlassButton` 样式，圆角为 8（与标签页一致）。

| 类型 | 用途 | 高度 | 宽度 | 示例 |
|------|------|------|------|------|
| 设置类 | 加载图片、选择路径 | 36 | 自适应 | 加载图片、选择路径 |
| 操作类 | 确认变更、清空所有、应用地图 | 44 | 160 | 确认变更、清空所有、应用地图 |
| 对话框 | 确定、取消、应用 | 40 | 100 | 确定、取消、应用 |

**使用示例：**

```xml
<!-- 设置类按钮 -->
<Button Content="📷 加载图片" 
        Style="{StaticResource GlassButton}" 
        Height="36" />

<!-- 操作类按钮 -->
<Button Content="✅ 确认变更" 
        Style="{StaticResource GlassButton}" 
        Height="44" 
        Width="160" />

<!-- 对话框按钮 -->
<Button Content="确定" 
        Style="{StaticResource GlassButton}" 
        Height="40" 
        Width="100" />
```

### 圆角规格

| 元素 | 圆角值 | 说明 |
|------|--------|------|
| 按钮 | 8 | GlassButton 样式 |
| 标签页 | 8 | GlassTabItem 样式 |
| 卡片 | 12 | 内部元素 |
| 主容器 | 16 | 玻璃卡片 |

### 边距规格

| 类型 | 值 | 用途 |
|------|-----|------|
| 紧凑 | 8 | 元素间距 |
| 标准 | 16 | 内容边距 |
| 宽松 | 20 | 主容器边距 |

---

## 调试功能

### 控制台调试模式

项目支持控制台调试模式，用于显示详细的调试日志输出。

#### 如何开启调试模式

在项目文件 `FS服装搭配专家v1.0.csproj` 中，修改 `OutputType` 属性：

```xml
<!-- 调试模式：显示控制台窗口 -->
<OutputType>Exe</OutputType>

<!-- 正常模式：隐藏控制台窗口（默认） -->
<OutputType>WinExe</OutputType>
```

#### 调试日志内容

开启调试模式后，控制台会显示以下信息：

1. **应用程序启动流程**
   - 窗口初始化状态
   - 配置加载过程
   - 皮肤管理器初始化

2. **服装数据加载**
   - PAK文件解包过程
   - 数据解析结果
   - 加载统计信息

3. **滚动条调试信息**
   - ScrollViewer属性值
   - 滚动位置变化
   - 布局更新状态

#### 使用场景

- 排查滚动条位置问题
- 调试数据加载流程
- 跟踪应用程序状态
- 性能分析

**注意**：调试完成后，记得将 `OutputType` 改回 `WinExe` 以隐藏控制台窗口。

---

### 17. 控制台开关功能（2026年3月8日）

**功能描述**：
- 标题栏右侧添加滑动开关，控制控制台窗口的显示/隐藏
- 开关状态持久化保存，下次启动时记住上次设置
- 使用 Win32 API 动态创建/销毁控制台窗口

**实现方式**：
- 创建 `Program.cs` 作为程序入口点，在 WPF 启动前处理控制台
- 使用 `AllocConsole()` 动态创建控制台
- 使用 `FreeConsole()` 销毁控制台
- 开关状态保存在 `console.ini` 文件中

**UI设计**：
- 位置：FreeStyle 标题右侧
- 尺寸：32×16 px（轨道），14×14 px（滑块）
- 颜色：关闭时灰色(#888888)，打开时绿色(#4CAF50)
- 动画：0.15秒平滑过渡

**操作方式**：
- 点击滑动开关：切换控制台显示/隐藏
- 状态自动保存，下次启动时恢复

**当前状态**：✅ 功能已实现

---

### 18. 特效添加功能（2026年3月8日）

**功能描述**：
- 服装列表和特效列表并排显示，各占 1/2 宽度
- 一个搜索框同时过滤服装列表和特效列表
- Alt+左键选中服装后，可为其添加特效
- 变更时自动将特效代码写入 itemshop.txt 并重新打包

**UI布局**：
```
┌─────────────────────┬─────────────────────┐
│     服装列表         │      特效列表        │
│   (原有功能)         │    (新增)           │
│                     │                     │
│  显示所有服装        │  显示有特效的服装    │
└─────────────────────┴─────────────────────┘
```

**操作方式**：

| 操作 | 功能 |
|------|------|
| **左键** | 添加到变更前列表 |
| **Ctrl+左键** | 添加到变更后列表 |
| **Alt+左键** | 选中变更后服装，并添加已选特效 |

**特效添加流程**：
1. 在特效列表中选择特效（显示特效名称）
2. Alt+左键点击服装列表中的服装
3. 特效代码会绑定到该服装
4. 点击"变更"按钮时：
   - 复制模型文件
   - 写入特效到 `itemshop.txt`
   - 重新打包 `item_text.pak`

**实现细节**：
- 特效列表从 itemshop.txt 筛选有特效代码的服装
- 特效显示格式：只显示特效名称（服装名称）
- 变更时修改 itemshop.txt 第3列（特效代码字段）
- 使用 GB2312 编码读写文件，避免乱码

**关键代码**：
```csharp
// 特效写入 itemshop.txt
var lines = File.ReadAllLines(itemshopPath, Encoding.GetEncoding("GB2312"));
for (int i = 0; i < lines.Length; i++)
{
    if (lines[i].StartsWith(beforeItem.ItemCode + "\t"))
    {
        string[] parts = lines[i].Split('\t');
        if (parts.Length >= 3)
        {
            parts[2] = afterEffectCode;  // 修改特效代码
            lines[i] = string.Join("\t", parts);
        }
    }
}
File.WriteAllLines(itemshopPath, lines, Encoding.GetEncoding("GB2312"));

// 重新打包 item_text.pak
string packTextCmd = "pack\\resources -file2pak \"" + itemTextPakDir + "\" \"" + itemTextPakPath + "\"";
conmon.RunCmd(packTextCmd);
```

**当前状态**：✅ 功能已实现

---

### 19. 性能监控代码（2026年3月8日）

**功能描述**：
- 在关键位置添加耗时日志
- 通过控制台输出性能数据
- 帮助诊断顿卡问题

**监控位置**：

| 方法 | 监控内容 |
|------|----------|
| `Search()` | 搜索文本获取、列表过滤、UI更新耗时 |
| `GetNewItem()` | 路径初始化、文件解析、配置加载、UI绑定耗时 |

**日志格式**：
```
[性能] Search - 获取搜索文本: Xms
[性能] Search - 过滤列表(服装: N -> M, 特效: K -> L): Xms
[性能] GetNewItem - 初始化路径: Xms
[性能] GetNewItem - 解析文件(N条): Xms
[性能] GetNewItem - 总耗时: Xms
```

**当前状态**：✅ 功能已实现

---

### 32. 路径管理与发布包优化（2026年3月10日）

**问题描述**：
- resources.exe 路径使用相对路径 `..\..\..\pack\resources.exe`，发布后找不到
- icon 目录检测只检查目录是否存在，空目录也会跳过加载

**解决方案**：

1. **修复 resources.exe 路径**：
   - 改为使用 `AppDomain.CurrentDomain.BaseDirectory + "pack\resources.exe"`
   - 两处代码都已修复

2. **修复 icon 目录检测逻辑**：
   - 原来：`Directory.Exists(iconDir)`
   - 现在：`Directory.Exists(iconDir) && Directory.GetFiles(iconDir).Length > 0`
   - 空目录不再被视为已加载

**修改文件**：
- `MainWindow.xaml.cs`：路径修复和检测逻辑优化

**当前状态**：✅ 问题已解决

---

### 33. 皮肤设置按钮闪烁修复（2026年3月10日）

**问题描述**：
- 皮肤设置窗口的按钮鼠标悬停时一直闪烁

**解决方案**：

1. **移除 Storyboard 动画**：
   - `VisualStateManager` 中的动画会导致不断重绘
   - 改用 `Trigger` 直接设置属性

2. **移除 DropShadowEffect**：
   - 阴影效果会加重渲染负担
   - 简化按钮模板

**修改文件**：
- `SkinWindow.xaml`：按钮样式重构

**当前状态**：✅ 问题已解决

---

### 34. 皮肤设置窗口主题适配（2026年3月10日）

**修改内容**：

1. **卡片透明度改为80%**：
   - `GlassCardColor` 从 `#15FFFFFF` 改为 `#33FFFFFF`
   - `Card.Background` 改为 `#CCFFFFFF`

2. **按钮跟随主题风格**：
   - 添加 `FontFamily` 和 `FontWeight` 绑定
   - 默认主题按钮背景：`#CCFFFFFF`，文字：`#FF333333`
   - 暗黑主题按钮背景：`#60404060`，文字：白色

3. **暗黑主题按钮优化**：
   - 提高按钮背景亮度
   - 添加 `HoverBackgroundColor` 和 `PressedBackgroundColor`

**修改文件**：
- `SkinWindow.xaml`：样式绑定
- `SkinWindow.xaml.cs`：主题颜色设置
- `ThemeLoader.cs`：默认主题配置

**当前状态**：✅ 功能已实现

---

**最后更新时间**：2026年3月15日
**更新人**：AI Assistant

### 42. ConfirmDialog 样式优化（2026年3月15日）

**问题描述**：
- ConfirmDialog 使用硬编码颜色，与主题系统不统一
- 按钮样式与主窗口玻璃拟态风格不一致
- 字体和颜色未跟随主题变化

**解决方案**：

1. **移除硬编码颜色**：
   - 移除 `CardBackgroundColor`、`CardBorderColor` 等硬编码资源
   - 改为使用主题系统的 `GlassCardColor`、`GlassBorderColor` 等资源

2. **统一按钮样式**：
   - 使用与主窗口一致的玻璃拟态按钮样式
   - 背景使用 `ButtonGlassColor`
   - 悬停效果使用 `ButtonGlassPressedColor`

3. **字体和颜色统一**：
   - 字体使用 `ThemeFontFamily` 和 `ThemeFontWeight`
   - 文字颜色使用 `TextColor`
   - 确保与主窗口字体和颜色保持一致

**修改文件**：
- `UI/Windows/ConfirmDialog.xaml` - 样式和资源优化

**当前状态**：✅ 功能已实现
**更新内容**：完成P2-P5任务（窗口尺寸配置化、UI元素尺寸统一样式、提示文本资源化、日志文件整合、Win32常量定义）

## 待办事项

| 优先级 | 任务 | 状态 | 完成时间 |
|--------|------|------|----------|
| P2 | 窗口尺寸配置化 | ✅ 已完成 | 2026年3月11日 |
| P3 | UI 元素尺寸统一样式 | ✅ 已完成 | 2026年3月11日 |
| P3 | 提示文本资源化 | ✅ 已完成 | 2026年3月11日 |
| P4 | 日志文件整合 | ✅ 已完成 | 2026年3月11日 |
| P5 | Win32 常量定义 | ✅ 已完成 | 2026年3月11日 |

### 任务说明

1. **P2: 窗口尺寸配置化**
   - 将所有窗口的默认尺寸存储到 `app.config.json`
   - 支持用户自定义窗口尺寸并保存
   - 实现窗口尺寸的记忆功能

2. **P3: UI 元素尺寸统一样式**
   - 创建统一的按钮、输入框、标签页样式
   - 确保所有UI元素尺寸一致
   - 减少硬编码的尺寸值

3. **P3: 提示文本资源化**
   - 将所有提示文本、错误消息等整合到资源文件
   - 支持多语言（可选）
   - 便于统一管理和修改

4. **P4: 日志文件整合**
   - 将多个日志文件合并为一个结构化日志
   - 添加日志级别控制
   - 优化日志存储和清理机制

5. **P5: Win32 常量定义**
   - 将 Win32 API 相关常量集中定义
   - 提高代码可读性
   - 便于维护和扩展

### 38. 配置文件合并优化（2026年3月10日）

**问题描述**：
- 应用程序使用多个独立的配置文件（config.ini、console.ini、current_skin.ini、pak_sizes.txt等）
- 文件分散，不便于管理和备份
- 配置读取代码分散在多个文件中

**解决方案**：

1. **创建统一配置系统**：
   - 新增 `Core/Config/AppConfiguration.cs` - 配置数据模型
   - 新增 `Core/Config/ConfigService.cs` - 配置服务（单例模式）
   - 新增 `Core/Config/AppConfig.cs` - 常量类

2. **配置文件合并**：
   | 旧文件 | 新位置 |
   |--------|--------|
   | config.ini | app.config.json → game.installDirectory |
   | console.ini | app.config.json → ui.consoleVisible |
   | current_skin.ini | app.config.json → ui.currentTheme |
   | pak_sizes.txt | app.config.json → data.pakSizes |
   | reference_bytes.txt | app.config.json → data.referenceBytes |

3. **自动迁移功能**：
   - ConfigService 首次运行时自动从旧配置文件迁移数据
   - 用户无需手动操作

4. **新增 app.config.json 结构**：
   ```json
   {
     "game": {
       "installDirectory": "C:\\Program Files (x86)\\T2CN\\街头篮球",
       "mapLibraryPath": ""
     },
     "ui": {
       "currentTheme": "default",
       "consoleVisible": false,
       "windowWidth": 1440,
       "windowHeight": 768
     },
     "data": {
       "pakSizes": {},
       "referenceBytes": {}
     }
   }
   ```

**修改文件**：
- `MainWindow.xaml.cs` - 使用 ConfigService 读取游戏目录
- `Program.cs` - 使用 ConfigService 读写控制台开关
- `SkinManager.cs` - 使用 ConfigService 读写当前主题
- `FillBytesWindow.xaml.cs` - 使用 ConfigService 读写 pak 尺寸

**删除的旧文件**：
- `config.ini`（项目根目录）

**当前状态**：✅ 功能已实现

---

### 39. 常量类创建（2026年3月10日）

**功能描述**：
- 创建 `AppConfig.cs` 常量类统一管理硬编码字符串
- 提高代码可维护性，便于统一修改

**常量分类**：

| 类别 | 常量示例 |
|------|----------|
| Directories | Cookies, Pack, Skins, Videos, Logs |
| Files | Config, DopakLog, OftenItemCode, ItemTextPak, ResourcesExe |
| WindowSize | MainWindowWidth/Height, BgCropWindowWidth/Height 等 |
| IconSize | ContainerSize, IconDisplaySize |
| Theme | DefaultThemeId, DarkThemeId, ThemeFileName |

**使用示例**：
```csharp
// 旧代码
private string cookiename = "cookies";
File.AppendAllText("dopaklog.ini", logEntry);

// 新代码
private string cookiename = AppConfig.Directories.Cookies;
File.AppendAllText(AppConfig.Files.DopakLog, logEntry);
```

**当前状态**：✅ 功能已实现

---

### 40. 子窗口主题接入（2026年3月10日）

**问题描述**：
- BgCropWindow、FillBytesWindow、ConfirmDialog 使用硬编码颜色
- 切换主题后，子窗口颜色不跟随变化

**解决方案**：

1. **XAML 颜色改为 DynamicResource**：
   - 所有 `StaticResource` 改为 `DynamicResource`
   - 硬编码颜色值（如 `#15FFFFFF`）改为资源绑定

2. **修改的窗口**：
   | 窗口 | 修改内容 |
   |------|----------|
   | BgCropWindow.xaml | 颜色、边框、背景全部改为 DynamicResource |
   | FillBytesWindow.xaml | 颜色、边框、背景全部改为 DynamicResource |
   | ConfirmDialog.xaml | 颜色、边框、背景全部改为 DynamicResource |

3. **主题资源映射**：
   | 资源名 | 用途 |
   |--------|------|
   | GlassCardColor | 卡片背景色 |
   | GlassBorderColor | 边框颜色 |
   | TextColor | 主文字颜色 |
   | StatusTextColor | 次要文字颜色 |
   | ButtonGlassColor | 按钮背景色 |
   | ThemeFontFamily | 字体族 |
   | ThemeFontWeight | 字重 |

**当前状态**：✅ 功能已实现

### 35. 取消窗口置顶设置（2026年3月10日）

**问题描述**：
- 应用程序默认所有窗口都置顶（Topmost="True"）
- 用户希望取消置顶，让窗口可以正常被其他窗口覆盖

**解决方案**：
将以下窗口的 `Topmost` 属性从 `True` 改为 `False`：
- MainWindow.xaml
- ConfirmDialog.xaml
- FillBytesWindow.xaml
- BgCropWindow.xaml

**当前状态**：✅ 问题已解决

---

### 36. 主题透明度平衡调整（2026年3月10日）

**问题描述**：
- 默认玻璃拟态主题透明度很高（80%不透明度），几乎不透明
- 暗黑霓虹主题透明度很低（0%不透明度），完全透明
- 两个主题透明度差异过大

**解决方案**：

**默认玻璃拟态主题调整**：
| 元素 | 原值 | 新值 | 说明 |
|------|------|------|------|
| Card.Background | `#CCFFFFFF` (80%) | `#66FFFFFF` (40%) | 降低不透明度 |
| Card.ContentPanelBackground | `#0DFFFFFF` (5%) | `#1AFFFFFF` (10%) | 略微提高 |
| Button.Background | `#CCFFFFFF` (80%) | `#66FFFFFF` (40%) | 降低不透明度 |

**暗黑霓虹主题调整**：
| 元素 | 原值 | 新值 | 说明 |
|------|------|------|------|
| Card.Background | `Transparent` (0%) | `#40FFFFFF` (25%) | 提高不透明度 |
| Card.ContentPanelBackground | `#10FFFFFF` (6%) | `#20FFFFFF` (12.5%) | 提高不透明度 |
| Button.Background | `#20FFFFFF` (12.5%) | `#40FFFFFF` (25%) | 提高不透明度 |
| ListItem.Background | `#15FFFFFF` | `#25FFFFFF` | 提高不透明度 |
| Tab.SelectedBackground | `#30FFFFFF` | `#40FFFFFF` | 提高不透明度 |
| TextBox.Background | `#15FFFFFF` | `#25FFFFFF` | 提高不透明度 |

**当前状态**：✅ 问题已解决

---

### 37. 隐藏 UnFSPak DOS 窗口（2026年3月10日）

**问题描述**：
- 每次启动程序时，UnFSPak 工具会弹出一个 DOS 窗口
- 显示解包过程信息，影响用户体验

**解决方案**：
修改 `MainWindow.xaml.cs` 中解包进程的启动配置：
```csharp
ProcessStartInfo processStartInfo = new ProcessStartInfo();
processStartInfo.FileName = resourcesExePath;
processStartInfo.Arguments = $"\"{destFileName}\" -all";
processStartInfo.UseShellExecute = false;  // 必须为 false 才能隐藏窗口
processStartInfo.CreateNoWindow = true;
processStartInfo.WindowStyle = ProcessWindowStyle.Hidden;
processStartInfo.RedirectStandardOutput = true;
processStartInfo.RedirectStandardError = true;
processStartInfo.WorkingDirectory = currentDir;
```

**关键点**：
- `UseShellExecute = false` 是必须的，否则 `CreateNoWindow = true` 不生效
- 添加 `WindowStyle = ProcessWindowStyle.Hidden` 双重保险
- 添加输出重定向以便调试

**当前状态**：✅ 问题已解决

---

### 20. 主题独立字体设置（2026年3月10日）

**功能描述**：
- 每个主题可以独立配置字体族（FontFamily）和字重（FontWeight）
- 暗黑主题默认使用思源黑体 Light 纤细字体
- 其他主题使用默认微软雅黑

**实现方式**：
- 在 `SkinTheme.TextStyle` 中增加 `FontFamily` 和 `FontWeight` 属性
- 在 `ThemeApplier` 中应用字体设置到窗口资源
- 所有 XAML 文件通过 `DynamicResource` 绑定字体

**主题字体配置示例**：
```json
{
  "styles": {
    "text": {
      "primary": "#FFFFFFFF",
      "secondary": "#B0FFFFFF",
      "title": "#FF6DD5FA",
      "body": "#E0FFFFFF",
      "fontFamily": "Source Han Sans SC",
      "fontWeight": "Light"
    }
  }
}
```

**支持的字重值**：
| 字重值 | 说明 |
|--------|------|
| Thin | 最细 |
| ExtraLight / UltraLight | 极细 |
| Light | 细体 |
| Normal | 常规 |
| Medium | 中等 |
| SemiBold | 半粗 |
| Bold | 粗体 |
| ExtraBold / UltraBold | 极粗 |
| Black | 最粗 |

**当前状态**：✅ 功能已实现

---

### 21. 统一字体绑定（2026年3月10日）

**问题描述**：
- 项目有多个独立的 XAML 文件（窗口/UserControl）
- 每个文件有自己的 Resources，字体设置不统一
- 切换主题后，部分窗口字体没有同步变化

**解决方案**：
- 所有 XAML 文件添加统一的字体资源定义
- 所有样式和 TextBlock 绑定 `DynamicResource` 字体
- SkinWindow 在加载时从当前主题同步字体

**已修改的文件**：
| 文件 | 修改内容 |
|------|----------|
| MainWindow.xaml | 字体资源 + 样式绑定 |
| MapControl.xaml | 字体资源 + 样式绑定 |
| SkinWindow.xaml | 字体资源 + 样式绑定 |
| SkinWindow.xaml.cs | 加载时同步当前主题字体 |
| ConfirmDialog.xaml | 字体资源 + 样式绑定 |
| BgCropWindow.xaml | 字体资源 + 样式绑定 |
| FillBytesWindow.xaml | 字体资源 + 样式绑定 |
| TeamSelectionWindow.xaml | 字体资源 + 样式绑定 |

**当前状态**：✅ 问题已解决

---

### 22. 列表项文字颜色设置（2026年3月10日）

**功能描述**：
- 每个主题可以独立配置列表项文字颜色
- 暗黑主题列表文字为白色

**实现方式**：
- 在 `SkinTheme.ListItemStyle` 中增加 `Foreground` 属性
- 在 `ThemeApplier` 中应用 `ListItemForegroundColor` 资源
- ListBox 样式绑定 `DynamicResource ListItemForegroundColor`

**当前状态**：✅ 功能已实现

---

### 23. 特效操作说明（2026年3月10日）

**功能描述**：
- 在服装列表下方增加操作说明文字
- 分两行显示：服装操作和特效操作

**UI显示**：
```
服装操作：左键添加到变更前 | Ctrl+左键添加到变更后
特效操作：先选特效，再Alt+左键点击服装即可添加
```

**当前状态**：✅ 功能已实现

---

### 24. SkinWindow 关闭按钮修复（2026年3月10日）

**问题描述**：
- 皮肤设置窗口的关闭按钮被部分遮挡
- 标题栏高度不足以容纳按钮

**解决方案**：
- 标题栏 Grid 高度从 40px 增加到 50px
- 移除 StackPanel 包装，直接放置按钮

**当前状态**：✅ 问题已解决

---

### 29. 暗黑主题视觉优化（2026年3月10日）

**问题描述**：
- 暗黑主题卡片透明度不够明显
- 白色字体在半透明白色卡片上看不清
- 卡片外发光效果不明显
- 字体太纤细

**解决方案**：

1. **卡片透明度调整**：
   - 卡片背景：`#40FFFFFF` → `#8CFFFFFF`（55%不透明度）
   - 边框颜色：`#50FFFFFF` → `#8CFFFFFF`
   - 内容面板：`#20FFFFFF` → `#8CFFFFFF`

2. **字体颜色改为黑色**：
   - Primary: `#FFFFFFFF` → `#E6000000`
   - Secondary: `#B0FFFFFF` → `#B0000000`
   - Title: `#FF6DD5FA` → `#E6000000`
   - Body: `#E0FFFFFF` → `#E0000000`
   - 按钮、列表项、标签页、文本框前景色同步调整

3. **卡片外发光效果增强**：
   - BlurRadius: 32 → 40
   - ShadowDepth: 10 → 0（围绕边框发光）
   - Opacity: 0.12 → 0.6
   - Color: `#000000` → `#6DD5FA`（霓虹蓝色）

4. **字体加粗**：
   - FontWeight: `Light` → `Bold`

**当前状态**：✅ 问题已解决

---

### 30. 暗黑主题文字颜色优化（2026年3月10日）

**问题描述**：
- 暗黑主题文字改为黑色后，在深色视频背景上难以辨认
- 卡片白色背景遮挡视频背景效果

**解决方案**：

1. **文字颜色改为白色**：
   - Primary: `#E6000000` → `#FFFFFFFF`
   - Secondary: `#B0000000` → `#B0FFFFFF`
   - Title: `#E6000000` → `#FFFFFFFF`
   - Body: `#E0000000` → `#E0FFFFFF`

2. **卡片背景透明融入背景**：
   - Background: `#8CFFFFFF` → `Transparent`
   - BorderColor: `#8CFFFFFF` → `#20FFFFFF`
   - ContentPanelBackground: `#8CFFFFFF` → `#10FFFFFF`
   - Shadow Opacity: `0.8` → `0.3`

3. **其他组件透明度降低**：
   - 按钮、列表项、标签页、文本框背景透明度统一降低
   - 文字颜色统一改为白色

**当前状态**：✅ 问题已解决

---

### 31. 列表界面优化（2026年3月10日）

**修改内容**：

1. **删除总标题**：
   - 移除"服装与特效列表"总标题，简化界面

2. **表头字号放大**：
   - "服装列表"和"特效列表"标题字号从 14 改为 15

3. **列表项字号放大**：
   - 为两个列表添加 ItemTemplate
   - 列表项字号从默认改为 15
   - 移除后台代码中的 `DisplayMemberPath` 设置

4. **表头添加图标前缀**：
   - 👕 服装列表
   - ✨ 特效列表
   - 便于区分两个列表

**修改文件**：
- `MainWindow.xaml`：UI布局调整
- `MainWindow.xaml.cs`：移除 DisplayMemberPath

**当前状态**：✅ 功能已实现

---

**最后更新时间**：2026年3月15日
**更新人**：AI Assistant

### 42. ConfirmDialog 简化优化（2026年3月15日）

**问题描述**：
- ConfirmDialog 背景透明度过高，几乎看不见
- 消息文本不显示，占用空间
- 对话框遮挡主界面

**解决方案**：

1. **简化为纯按钮模式**：
   - 移除背景卡片和消息文本
   - 只保留"取消"和"确定"两个按钮
   - 窗口背景完全透明

2. **定位优化**：
   - 对话框定位在"确认变更"按钮正下方
   - 宽度与确认变更按钮一致（160px）
   - 有"延伸跳出"的视觉效果

3. **按钮布局**：
   - 两个按钮各 72px 宽，间距 6px
   - 高度 36px，字体 13px
   - 白色半透明玻璃拟态风格

**修改文件**：
- `UI/Windows/ConfirmDialog.xaml` - 简化为纯按钮模式
- `UI/Windows/ConfirmDialog.xaml.cs` - 移除消息文本设置
- `UI/Windows/MainWindow.xaml.cs` - 添加按钮下方定位逻辑

**当前状态**：✅ 功能已实现

### 43. 篮球弹跳加载动画（2026年3月15日）

**问题描述**：
- 程序启动时服装列表加载需要几秒钟
- 用户等待时没有视觉反馈
- 需要吸引用户注意力的加载动画

**解决方案**：

1. **动画效果**：
   - 使用程序图标（favicon.ico）作为篮球
   - 模拟真实物理弹跳：重力加速度下落 + 减速弹起
   - 4 次弹跳，高度线性递减
   - 落地后有余震效果
   - 球体旋转动画（720度/周期）

2. **物理模拟**：
   - 下落使用 `QuadraticEase EaseIn`（加速运动）
   - 弹起使用 `QuadraticEase EaseOut`（减速运动）
   - 弹跳高度递减：-25 → -12 → -5 → 余震

3. **动画时间线**：
   - 第一次下落：0.5 秒
   - 完整周期：约 3 秒
   - 循环播放直到加载完成

4. **技术实现**：
   - 使用 WPF Storyboard 动画
   - TransformGroup 组合旋转和位移
   - 图标作为嵌入资源加载

**修改文件**：
- `UI/Windows/MainWindow.xaml` - 添加动画 XAML 定义
- `UI/Windows/MainWindow.xaml.cs` - 添加动画控制方法
- `FS服装搭配专家v1.0.csproj` - 添加图标资源引用

**当前状态**：✅ 功能已实现

## 待办事项

| 优先级 | 任务 | 状态 | 计划完成时间 |
|--------|------|------|--------------|
| P2 | 窗口尺寸配置化 | 未开始 | 2026年3月12日 |
| P3 | UI 元素尺寸统一样式 | 未开始 | 2026年3月13日 |
| P3 | 提示文本资源化 | 未开始 | 2026年3月13日 |
| P4 | 日志文件整合 | 未开始 | 2026年3月14日 |
| P5 | Win32 常量定义 | 未开始 | 2026年3月15日 |

### 任务说明

1. **P2: 窗口尺寸配置化**
   - 将所有窗口的默认尺寸存储到 `app.config.json`
   - 支持用户自定义窗口尺寸并保存
   - 实现窗口尺寸的记忆功能

2. **P3: UI 元素尺寸统一样式**
   - 创建统一的按钮、输入框、标签页样式
   - 确保所有UI元素尺寸一致
   - 减少硬编码的尺寸值

3. **P3: 提示文本资源化**
   - 将所有提示文本、错误消息等整合到资源文件
   - 支持多语言（可选）
   - 便于统一管理和修改

4. **P4: 日志文件整合**
   - 将多个日志文件合并为一个结构化日志
   - 添加日志级别控制
   - 优化日志存储和清理机制

5. **P5: Win32 常量定义**
   - 将 Win32 API 相关常量集中定义
   - 提高代码可读性
   - 便于维护和扩展
