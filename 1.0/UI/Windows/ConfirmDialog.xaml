<Window x:Class="FS服装搭配专家v1._0.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认"
        Height="54"
        Width="160"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="False"
        ShowInTaskbar="False">
    <Window.Resources>
        <SolidColorBrush x:Key="DialogButtonColor" Color="#CCFFFFFF" />
        <SolidColorBrush x:Key="DialogButtonHoverColor" Color="#B2FFFFFF" />
        <SolidColorBrush x:Key="DialogBorderColor" Color="#4D000000" />
        <SolidColorBrush x:Key="DialogTextColor" Color="#FF333333" />
        
        <Style x:Key="GlassButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DialogButtonColor}" />
            <Setter Property="Foreground" Value="{StaticResource DialogTextColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource DialogBorderColor}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource DialogButtonHoverColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
            <Button Content="取消" 
                    Width="72"
                    Height="36"
                    Style="{StaticResource GlassButton}"
                    Click="Cancel_Click" 
                    Margin="0,0,6,0" />
            <Button Content="确定" 
                    Width="72"
                    Height="36"
                    Style="{StaticResource GlassButton}"
                    Click="Confirm_Click" />
        </StackPanel>
    </Grid>
</Window>
