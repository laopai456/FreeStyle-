<Window x:Class="FS服装搭配专家v1._0.SkinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="皮肤设置"
        Height="550"
        Width="700"
        MinHeight="500"
        MinWidth="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <!-- 颜色资源 -->
        <SolidColorBrush x:Key="GlassCardColor" Color="#33FFFFFF" />
        <SolidColorBrush x:Key="GlassCardHoverColor" Color="#40FFFFFF" />
        <SolidColorBrush x:Key="GlassBorderColor" Color="#4DFFFFFF" />
        <SolidColorBrush x:Key="GlassHighlightColor" Color="#20FFFFFF" />
        <SolidColorBrush x:Key="TextColor" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="TitleTextColor" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="StatusTextColor" Color="#FF999999" />
        <SolidColorBrush x:Key="ButtonGlassColor" Color="#25FFFFFF" />
        <SolidColorBrush x:Key="ButtonGlassPressedColor" Color="#40FFFFFF" />
        <SolidColorBrush x:Key="ButtonForegroundColor" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="BodyTextColor" Color="#E6FFFFFF" />
        <SolidColorBrush x:Key="HoverBackgroundColor" Color="#40FFFFFF" />
        <SolidColorBrush x:Key="PressedBackgroundColor" Color="#40FFFFFF" />
        <SolidColorBrush x:Key="ListItemBackgroundColor" Color="#1AFFFFFF" />
        <SolidColorBrush x:Key="ListItemHoverBackgroundColor" Color="#33FFFFFF" />
        
        <!-- 字体资源 -->
        <FontFamily x:Key="ThemeFontFamily">Microsoft YaHei</FontFamily>
        <FontWeight x:Key="ThemeFontWeight">Normal</FontWeight>
        
        <!-- 主容器样式 -->
        <Style x:Key="MainContainer" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource GlassCardColor}" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Padding" Value="24" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightColor}" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="40" 
                                      ShadowDepth="12" 
                                      Opacity="0.15" 
                                      Direction="270" 
                                      Color="#000000" />
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 标题栏容器样式 -->
        <Style x:Key="TitleBarContainer" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
        
        <!-- 顶部标题栏卡片样式 -->
        <Style x:Key="TitleBarCard" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        
        <!-- 
            玻璃拟态按钮样式 - GlassButton
            用途：所有按钮的统一样式
            圆角：8（与标签页一致）
            
            按钮规格：
            - 对话框按钮（应用、取消）：Height="40" Width="100"
            - 关闭按钮：Height="32" Width="70"
        -->
        <Style x:Key="GlassButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonGlassColor}" />
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightColor}" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="{DynamicResource ThemeFontFamily}" />
            <Setter Property="FontWeight" Value="{DynamicResource ThemeFontWeight}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter x:Name="ContentPresenter" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              TextElement.Foreground="{TemplateBinding Foreground}" 
                                              TextElement.FontFamily="{TemplateBinding FontFamily}"
                                              TextElement.FontWeight="{TemplateBinding FontWeight}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource HoverBackgroundColor}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource PressedBackgroundColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 窗口管理按钮样式 -->
        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonGlassColor}" />
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightColor}" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="Margin" Value="6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="60" />
            <Setter Property="FontFamily" Value="{DynamicResource ThemeFontFamily}" />
            <Setter Property="FontWeight" Value="{DynamicResource ThemeFontWeight}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter x:Name="ContentPresenter" 
                                              Content="{TemplateBinding Content}"
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontSize="12"
                                              TextElement.FontFamily="{TemplateBinding FontFamily}"
                                              TextElement.FontWeight="{TemplateBinding FontWeight}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource HoverBackgroundColor}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource PressedBackgroundColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 皮肤项样式 -->
        <Style x:Key="SkinItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ListItemBackgroundColor}" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderColor}" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" 
                                      ShadowDepth="2" 
                                      Opacity="0.05" 
                                      Direction="270" 
                                      Color="#000000" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ListItemHoverBackgroundColor}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <!-- 渐变背景 -->
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#6a85b6" Offset="0" />
                <GradientStop Color="#bac8e0" Offset="0.5" />
                <GradientStop Color="#f0c9e8" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>
        
        <!-- 主内容区域 -->
        <Border Style="{StaticResource MainContainer}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
            
            <!-- 顶部标题栏卡片 -->
            <Border Grid.Row="0" Style="{StaticResource TitleBarContainer}" 
                    Background="Transparent" 
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="50">
                    <TextBlock Text="皮肤设置" 
                               FontSize="18" 
                               FontFamily="{DynamicResource ThemeFontFamily}"
                               FontWeight="{DynamicResource ThemeFontWeight}"
                               Foreground="{DynamicResource TitleTextColor}"
                               VerticalAlignment="Center" 
                               HorizontalAlignment="Left" />
                    <Button Content="关闭" 
                            Width="70" 
                            Height="32"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Style="{StaticResource GlassButton}" 
                            Click="Close_Click" />
                </Grid>
            </Border>
            
            <!-- 中间内容 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <!-- 左侧皮肤列表 -->
                <Border Grid.Column="0" Padding="16" CornerRadius="16" Background="Transparent">
                    <Grid>
                        <TextBlock Text="可用皮肤" 
                                   FontSize="16" 
                                   FontFamily="{DynamicResource ThemeFontFamily}"
                                   FontWeight="{DynamicResource ThemeFontWeight}"
                                   Foreground="{DynamicResource TextColor}"
                                   Margin="0,0,0,16" />
                        <ListBox x:Name="lstSkins" 
                                 Margin="0,40,0,0" 
                                 SelectionChanged="lstSkins_SelectionChanged"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource SkinItemStyle}">
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="14" 
                                                   FontFamily="{DynamicResource ThemeFontFamily}"
                                                   FontWeight="{DynamicResource ThemeFontWeight}"
                                                   Foreground="{DynamicResource TextColor}" />
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.Resources>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Resources>
                        </ListBox>
                    </Grid>
                </Border>
                
                <!-- 右侧皮肤预览 -->
                <Border Grid.Column="1" Padding="16" CornerRadius="16" Background="Transparent">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="皮肤预览" 
                                   FontSize="16" 
                                   FontFamily="{DynamicResource ThemeFontFamily}"
                                   FontWeight="{DynamicResource ThemeFontWeight}"
                                   Foreground="{DynamicResource TextColor}"
                                   Margin="0,0,0,16" />
                        
                        <Border Grid.Row="1" x:Name="previewBorder" 
                                Height="160" 
                                CornerRadius="12" 
                                BorderThickness="1" 
                                BorderBrush="{DynamicResource GlassHighlightColor}">
                            <Grid x:Name="previewGrid">
                                <TextBlock Text="预览区域" 
                                           FontSize="14" 
                                           Foreground="{DynamicResource TextColor}"
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        
                        <TextBlock Grid.Row="2" x:Name="txtSkinInfo" 
                                   FontSize="13" 
                                   Foreground="{DynamicResource BodyTextColor}"
                                   Margin="0,16,0,0"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Top" />
                    </Grid>
                </Border>
            </Grid>
            
            <!-- 底部按钮 -->
            <Grid Grid.Row="2">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center">
                    <Button Content="应用" 
                            Width="100"
                            Height="40"
                            Style="{StaticResource GlassButton}" 
                            Click="Apply_Click" 
                            Margin="0,0,10,0" />
                    <Button Content="取消" 
                            Width="100"
                            Height="40"
                            Style="{StaticResource GlassButton}" 
                            Click="Cancel_Click" />
                </StackPanel>
            </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>